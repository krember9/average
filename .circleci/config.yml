version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: ping 1.1.1.1

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
